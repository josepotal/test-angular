To run correctly the application you should 

`bower install`
in order to install all the dependencies used.

Then, we run a server for angular routes to work, the simplest option is just typing:

`http-server`

